import React from 'react';

const Navbar = (props) => {
  return (
    <div>
      <label>nav option</label>
      <label>nav option</label>
      <label>nav option</label>
    </div>
  )
}

export default Navbar;
